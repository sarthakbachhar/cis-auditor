# Core CIS Auditor Dependencies
ansible-runner>=2.3.3
ansible>=6.0.0
jinja2>=3.1.0
pyyaml>=6.0

# Web API Dependencies
flask>=2.3.0
flask-cors>=4.0.0

# gRPC Dependencies (optional)
grpcio>=1.50.0
grpcio-tools>=1.50.0
protobuf>=4.21.0

# GUI Dependencies (optional - only needed if using tkinter interface)
# tkinter comes with Python, but some systems may need:
# python3-tk (install via apt on Ubuntu/Debian)

# Additional utilities
requests>=2.28.0  # For client examples
uuid  # Built-in Python module
reportlab
